#pragma once

#include <kaguya/kaguya.hpp>

namespace obe
{
    namespace Bindings
    {
        namespace EditorBindings
        {
            void LoadEditorGrid(kaguya::State& lua);
        }
    }
}